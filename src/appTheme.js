import {createTheme} from "@mui/material";

const appTheme = createTheme({

    palette:{
        primary:{
            main: '#688ab6'
        },
        secondary: {
            main:'#dcdcdc'
        }
    },
    shape: {
      borderRadius: 5,
    },
})

export default appTheme;